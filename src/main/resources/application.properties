# Database connection settings for local PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/personal_finance_db
spring.datasource.username=postgres
spring.datasource.password=Lucknow@2801
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (Java Persistence API) settings
spring.jpa.hibernate.ddl-auto=update # <<-- Creates/updates tables automatically during development. Change to 'none' for production.
spring.jpa.show-sql=true # <<-- Shows SQL queries in console (helpful for debugging)
spring.jpa.properties.hibernate.format_sql=true # <<-- Formats SQL nicely in console
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect